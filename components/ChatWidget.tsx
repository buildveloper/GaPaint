'use client'; import {useState} from 'react'; const qa:any={services:'We offer collision repair, paint, dents, frame straightening, glass repair, and insurance support.',pricing:'Pricing depends on damage. Every estimate is free and transparent.',estimates:'Yes—free estimates are available now.',booking:'Call (770) 932-0742 or send a form request to book.',timelines:'Timelines depend on parts and claim approvals; we provide updates throughout.',areas:'We serve Suwanee and greater Atlanta/Gwinnett.'}; export default function ChatWidget(){const [o,setO]=useState(false); const [r,setR]=useState('Welcome! Ask about services, pricing, estimates, booking, timelines, or service areas.'); return <div className='fixed bottom-4 right-4 z-50'><button className='btn-primary' onClick={()=>setO(!o)}>{o?'Close':'AI Assistant'}</button>{o&&<div className='glass mt-2 w-80 p-4'><p className='text-sm'>{r}</p><div className='flex flex-wrap gap-2 mt-3'>{Object.keys(qa).map(k=><button key={k} className='px-2 py-1 text-xs bg-slate-100 dark:bg-slate-800 rounded' onClick={()=>{setR('Typing...');setTimeout(()=>setR(qa[k]),500)}}>{k}</button>)}</div></div>}</div>}
